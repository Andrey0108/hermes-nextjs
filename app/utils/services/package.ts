import { LOCAL } from "@/app/utils/env";

const url = `${LOCAL}/package`;

export const getAll = () => {
    return null;
};

export const getById = (id: number) => {
    return null;
};

export const create = () => {
    return null;
};

export const update = (id: number) => {
    return null;
};

export const modify = (id:number) => {
    return null;
}

export const remove = (id: number) => {
    return null;
};